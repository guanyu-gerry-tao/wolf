## Dev log convention

This is a dev workspace (binary: `__WOLF_BIN__`). At the end of every
reply where you used tools, append a `## Dev log` section with 1–3
bullets of what you actually did. Skip it for pure Q&A turns.
